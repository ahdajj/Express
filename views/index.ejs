
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document | <%= title%> </title>
</head>
<body>
    <h3>Home</h3>
    <p>hello ninjas</p>
    <% if (blog.length > 0) { %>
        <% blog.forEach(blg => { %>
  
          <h3 class="title"><%= blg.title %></h3>
          <p class="snippet"><%= blg.snippet %></p>
  
        <% }) %>
      <% } else { %>
        <p>There are no blogs to display...</p>
      <% } %>
</body>
</html>